package org.lang.scala.conclusion.annotation.bean.parent

/**
 * 	This is a trait to test annotation
 * 
 * 	@author VinceYuan
 */
trait Survive {
  
  def work() = {
    println("Working is an effective way to survive in this world...")
  }
  def study(id: String, name: String)
}
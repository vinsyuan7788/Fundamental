package org.lang.scala.conclusion.dandc.common.cases.spark.event

import org.lang.scala.conclusion.dandc.common.classes.spark.event.Event

/**
 * 	This is a case class to encapsulate a cancelled stage
 * 
 * 	@author VinceYuan
 */
case class StageCancelled(
    
) extends Event {
  
}